export async function runMemoryDemo() {
  console.log("\n--- Memory Demo ---");

  console.log(
    "Memory Example: Earlier the user mentioned learning LangChain, so the assistant recommends learning agents next."
  );
}
